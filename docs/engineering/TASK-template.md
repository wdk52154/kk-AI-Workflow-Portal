# TASK 规范模板

> 复制本文件到 `docs/{frontend,backend}/tasks/TASK-XXX-xxx.md` 后填写。
> 每个 TASK 对应一次 AI 驾驭编程 Session。

---

## 元信息

| 字段 | 值 |
|------|---|
| TASK ID | TASK-XXX |
| 标题 | [一句话描述] |
| 负责人 | [@username] |
| 优先级 | P0 / P1 / P2 |
| 预估工时 | Xh |
| 关联 PRD | [链接] |
| 关联 ARCH | [链接] |

---

## 1. 背景（Situation）

[1-2 句话说明为什么要做这个任务，链接到上游 PRD/需求文档]

---

## 2. 目标（Task）

[一句话清晰描述交付物]

---

## 3. 验收标准（Acceptance Criteria）

必须全部勾选才能关闭本 TASK：

- [ ] AC-1：[具体可验证的条件]
- [ ] AC-2：[具体可验证的条件]
- [ ] AC-3：[具体可验证的条件]

---

## 4. 技术方案（Action）

### 4.1 目录/文件变更

```
新增：
- path/to/new-file.ts

修改：
- path/to/existing-file.ts

删除：
- path/to/deprecated-file.ts
```

### 4.2 接口/API 定义

```typescript
// 如果是前端，写明组件 props 或 API 调用方式
interface XxxRequest {
  // ...
}

interface XxxResponse {
  // ...
}
```

### 4.3 关键逻辑

[描述核心算法或业务逻辑，必要时用伪代码]

### 4.4 约束条件

- [ ] 不使用 `any` 类型
- [ ] 所有新增函数必须有 JSDoc / docstring
- [ ] 错误处理必须包含 trace_id
- [ ] [其他项目特定约束]

---

## 5. 依赖与阻塞

| 依赖项 | 状态 | 说明 |
|--------|------|------|
| TASK-YYY | ⬜ 待完成 | 需要其输出的类型定义 |
| API `/xxx` | ✅ 已完成 | 后端接口已就绪 |

---

## 6. 测试策略

### 6.1 单元测试

- [ ] 测试场景 1：[输入] → [期望输出]
- [ ] 测试场景 2：[输入] → [期望输出]

### 6.2 集成测试

- [ ] curl / 页面访问验证步骤

---

## 7. 给 AI 的 Prompt（可选）

[将本 TASK 转换为可直接发给 AI 的 prompt，供下次复用]

```markdown
【Situation】
...

【Task】
...

【Action】
...

【Result】
...
```

---

## 8. 迭代记录

| 轮次 | AI 输出 | 人验收结果 | 修复点 |
|------|---------|-----------|--------|
| R1 | 初始实现 | ⚠️ 部分通过 | AC-2 边界条件未处理 |
| R2 | 修复边界 | ✅ 通过 | - |

---

## 9. 复盘与规范升级

[任务完成后填写]

- AI 理解规范的难点：
- 规范中需要补充的约束：
- 下次同类任务的建议：
